import { INHERIT_MODEL_VALUE } from '../subAgent';

describe('subAgent constants', () => {
  it('exports INHERIT_MODEL_VALUE as "inherit"', () => {
    expect(INHERIT_MODEL_VALUE).toBe('inherit');
  });
});
