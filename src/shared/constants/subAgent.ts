/**
 * Sub-agent shared constants used by both main and renderer processes.
 */

/**
 * Sentinel value indicating a sub-agent should inherit its model from the parent agent.
 * Stored in `SubAgentConfig.model` and surfaced as the default option in the model picker.
 */
export const INHERIT_MODEL_VALUE = 'inherit';
